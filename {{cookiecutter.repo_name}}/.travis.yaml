language: python
sudo: false
cache: pip
python: 3.5

env:
  - TOXENV=static
  - TOXENV=metrics
  - TOXENV=py{{ cookiecutter.min_python|replace(".", "") }}-test

install: pip install tox
script: tox

notifications:
  email:
    - {{ cookiecutter.email }}
